class User {
    constructor(user_id, name, password, email_address) {
      this.user_id = user_id;
      this.name = name;
      this.password = password;
      this.email_address = email_address;
    }
  }